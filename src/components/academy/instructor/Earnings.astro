---
const earnings = {
    total: 12500.00,
    last_payout: 3500.00,
    next_payout: 4200.00,
};
---

<div class="bg-white rounded-lg shadow-md p-6">
    <h2 class="text-xl font-bold mb-4">Ganhos</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-center">
        <div>
            <p class="text-gray-600">Ganhos Totais</p>
            <p class="text-2xl font-bold">R$ {earnings.total.toFixed(2)}</p>
        </div>
        <div>
            <p class="text-gray-600">Último Pagamento</p>
            <p class="text-2xl font-bold">R$ {earnings.last_payout.toFixed(2)}</p>
        </div>
        <div>
            <p class="text-gray-600">Próximo Pagamento</p>
            <p class="text-2xl font-bold">R$ {earnings.next_payout.toFixed(2)}</p>
        </div>
    </div>
    <div class="mt-6 text-center">
        <button class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">Ver Histórico de Pagamentos</button>
    </div>
</div>