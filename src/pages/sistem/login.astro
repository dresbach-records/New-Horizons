---
import PublicLayout from "../../layouts/PublicLayout.astro";
---

<PublicLayout title="Login | HORIZON System">
    <div class="w-full max-w-md">
        <div class="mb-12 text-center">
            <h3 class="text-4xl font-extrabold text-white mb-3 tracking-tight">Login do Sistema</h3>
            <p class="text-slate-400 font-medium">Insira suas credenciais para acessar o painel.</p>
        </div>
        <form class="space-y-6" action="/sistem/HORIZON-System/">
            <div>
                <label class="block text-xs font-bold text-slate-400 uppercase tracking-widest mb-3" for="email">E-mail Corporativo</label>
                <input class="input-dark w-full" id="email" name="email" placeholder="nome@horizon.com" required type="email" />
            </div>
            <div>
                <div class="flex justify-between items-center mb-3">
                    <label class="block text-xs font-bold text-slate-400 uppercase tracking-widest" for="password">Senha</label>
                    <a class="text-xs font-bold text-accent hover:text-white transition-colors" href="/sistem/recuperar-senha">Recuperar acesso</a>
                </div>
                <input class="input-dark w-full" id="password" name="password" placeholder="••••••••••••" required type="password" />
            </div>
            <div class="flex items-center">
                <input class="h-5 w-5 bg-white/5 border-white/10 text-accent rounded focus:ring-accent/30 focus:ring-offset-0" id="remember-me" name="remember-me" type="checkbox" />
                <label class="ml-3 block text-sm text-slate-400 font-medium" for="remember-me">Manter sessão ativa</label>
            </div>
            <button type="submit" class="w-full bg-accent hover:bg-accent/80 text-slate-900 font-extrabold py-4 rounded-xl shadow-[0_10px_30px_-10px_rgba(34,197,94,0.4)] transition-all transform active:scale-[0.98] flex items-center justify-center gap-3 text-base">
                <span>Entrar no Sistema</span>
                <span class="material-symbols-outlined font-bold">arrow_forward</span>
            </button>
        </form>
        <div class="mt-12 pt-8 border-t border-white/10">
            <p class="text-center text-sm text-slate-500 font-medium">
                Ainda não possui acesso? 
                <a class="text-white hover:text-accent font-bold transition-colors ml-1" href="/sistem/solicitar-acesso">Solicitar Credenciais</a>
            </p>
        </div>
    </div>
</PublicLayout>
